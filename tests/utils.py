"""
Important setup information for the test suite
"""
import os
import sys

# add the package directory to the path so we can import it
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

